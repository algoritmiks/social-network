import React from 'react';
import css from './Music.module.css';

const Music = (props) => {
    return (
        <div className = {css.item}>
            Music
        </div>
    );
}

export default Music;